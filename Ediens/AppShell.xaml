<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Ediens.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Ediens"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
       
        <Tab Title="Home">
            <ShellContent
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="MainPage" />
        </Tab>
        
        <Tab Title="Shelf" >
            <ShellContent
                ContentTemplate="{DataTemplate local:ShelfPage}"
                Route="ShelfPage" />
        </Tab>

        <Tab Title="Stats" >
            <ShellContent
                ContentTemplate="{DataTemplate local:GraphPage}"
                Route="GraphPage" />
        </Tab>
    
    </TabBar>

</Shell>
